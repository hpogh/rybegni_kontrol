<Window x:Class="_23102_Formova_3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Дисциплины - Вариант 3" Height="450" Width="600"
        MinWidth="400" MinHeight="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Название дисциплины:" VerticalAlignment="Center"/>
            <TextBox x:Name="SearchBox" Width="200" Margin="5,0"/>
            <Button x:Name="SearchButton" Content="Поиск" Width="80" Click="SearchButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Сортировка:"/>
            <ComboBox x:Name="SortCombo" Width="250" SelectedIndex="0">
                <ComboBoxItem Content="По возрастанию названия дисциплины"/>
                <ComboBoxItem Content="По убыванию названия дисциплины"/>
            </ComboBox>
        </StackPanel>

        <DataGrid x:Name="LoadData" Grid.Row="2" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</Window>